spring:
  application:
    name: stock-service

server:
  port: 8004
  
eureka:
  client:
   register-with-eureka: true
   fetch-registry: true
   service-url:
      defaultZone: http://localhost:8005/eureka
  instance:
      hostname: Phani